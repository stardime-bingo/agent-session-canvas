# 夜间冲刺日志

> 施工时间：2026-07-18 夜间；2026-07-19 继续收口证据缺口
>
> 分支：`feat/scene-store`
>
> 边界：未改写 `data/`、`~/.claude`、`~/.codex`；未操作 main、PR、Release；4517 只做构建生效、规范 kickstart 与只读验收。

## 0. 开工基线

- **做了什么**：逐字对齐 `docs/顶级状态计划.md`、根/前后端 CLAUDE 成员图与今天十笔提交；确认分支和正式 4517 健康后开工。
- **证据**：开工 `npm run verify:global` 为 65/65；4517 根页面与 `/api/graph` 均为 HTTP 200。
- **取舍**：先补 A 组高频手感，再修整理/同步，最后清结构和公开叙事；不在真实 4517 做任何坐标自动化。

## 1. 墨迹选中手感补全

- **做了什么**：新增 `ink-selection.js` 纯内核；选择态支持框选、Shift 多选、批量移动/删除/改样式、八向缩放、共同中心旋转、Cmd/Ctrl+C/V、Alt 拖复制。绑定宿主与文字按闭包一起变换，所有连续手势都 coalesce 为一步 undo。
- **证据**：提交 `c3018eb feat(ink): 补齐多选缩放旋转与复制手感`；选择纯函数测试和 4518 production FlowCanvas 链通过；该笔 `verify:global` 71/71。
- **取舍**：几何、闭包、ID/绑定重映射都留在纯函数，不把选择规则塞回 FlowCanvas；ClipboardEvent 同时写私有 MIME 与 text fallback，支持浏览器内跨会话画布粘贴。
- **未竟**：没有承诺跨应用通用矢量格式；当前跨会话指同一应用/画布上下文中的系统剪贴板。

## 2. 图片粘贴与拖入

- **做了什么**：图片文件粘贴/拖入时同步放可见占位，FileReader、解码和 SHA-256 内容寻址全部后台完成；成功后回填既有 `drawingFiles` 仓，失败显示元素级错误；用户先 undo 时，迟到异步结果不能复活元素。
- **证据**：提交 `fe9199d feat(ink): 图片粘贴拖入即时占位后台入仓`；图片尺寸、稳定内容 ID、同步占位测试通过；4518 production 图片链通过；该笔 `verify:global` 74/74。
- **取舍**：图片原始内容不先进入场景引用，继续遵守“资产先行、引用后到”；交互路径没有 await。
- **未竟**：没有加入裁剪、滤镜、压缩策略，这些不属于本轮顶级状态判据。

## 3. 整理同曲线与同步状态

- **做了什么**：接通已有但未使用的 batch carry 逆向 FLIP 桥；整理时所属墨迹先钉回旧像素，再与容器用同一 420ms 曲线滑到新位。SceneStore 同步状态改成稳定快照并单独订阅，TopBar 增加克制的 saved/dirty/saving/error 状态点。
- **证据**：提交 `f76ca2c fix(canvas): 整理墨迹同曲线并亮同步状态`；新增状态订阅与整理桥回归；该笔 `verify:global` 76/76。
- **取舍**：没有改写整理布局算法和人工归属，只修“节点滑、墨迹跳”的表征裂缝；状态点不弹窗、不阻塞、不要求用户重试。
- **未竟**：尚未用一组真实审美基线重新裁定“活跃者靠前”的整体排布节奏。

## 4. 橡皮与 4518 验收闭环

- **做了什么**：加入 `E` 橡皮工具；按住划过同步命中顶层元素，一整笔合并为一步 undo。4518 扩为十五链：冷渲、连发即时、DOM 同步、V/P/E 快捷键、框选多选、批量移动、缩放、旋转、复制粘贴、Alt 拖、图片粘贴、橡皮撤销、删除撤销、后台冲刷、console clean。
- **证据**：提交 `e9b013b feat(ink): 加入单笔橡皮并清除旧运行时`；4518 production 15/15，console error/warning、page error、失败请求、外联、`/api` 资源全部为 0。
- **取舍**：第一次夹具运行暴露了“旋转复制件盖住原件而断言盯错对象”的假失败；改用空间唯一的导入图片验证顶层命中，没有改生产命中规则去迎合测试。
- **未竟**：橡皮当前删除整元素，不做路径分割；这是本轮保持简单和可撤销性的明确边界。

## 5. 性能红线与结构卫生

- **做了什么**：恢复 300/800 两档自研墨迹直渲门；把连接说明、容器命中与绘图菜单移入既有纯模块；删除主线残留的旧绘图库相机/手势/样式运行时与对应失效测试；`FlowCanvas.jsx` 从 857 行降到 798 行。
- **证据**：4518 Chrome 冷挂载 300 元素 214ms（红线 900ms）、800 元素 214ms（红线 1600ms），DOM 数量精确一致且所有浏览器诊断为 0；`wc -l web/src/canvas/FlowCanvas.jsx` = 798；主线 `rg` 旧绘图库品牌/运行时残词为 0（只排除 `tests/archive/`、冻结历史计划与本任务书）。
- **取舍**：全局测试数从中途 76 降到最终 67，是删除 9 条已无生产入口的 v17 相机事务测试，不是隐藏失败；历史合同完整保留在 `tests/archive/`。
- **未竟（DEFERRED）**：352 节点拖动 60fps 还没有真实 Performance trace。不能拿 300/800 墨迹挂载代理冒充；建议新增 4518 的 352 节点专用场景并录 trace，仍不触碰 4517 坐标。

## 6. 文档与开源脸面

- **做了什么**：README 重写自研 SVG 墨迹、单场景文档、零等待、全套操作和本地图片资产说明；Codex Marketplace release notes 改成 v18/v19；PRIVACY/TERMS 明确内容寻址图片和本地后台持久化；根 AGENTS/CLAUDE、web/server 成员图同步；4518 文档改为十五链。
- **证据**：提交 `20078e9 docs: 用自研墨迹重写公开叙事`；主线残词扫描清零；公开文档与代码当前功能逐项对齐。
- **取舍**：没有伪造产品截图，也没有拿含真实会话的 4517 截图进仓。
- **未竟（DEFERRED）**：README 截图/GIF 尚未更新。建议在 4518 做一套专门的匿名 hero fixture 后再拍，避免真实会话泄露，也避免把验收夹具当产品广告图。

## 7. 稳定性证据边界

- **CONFIRMED**：SceneStore 的同步 mutate、coalesce undo/redo、防抖冲刷、失败无限退避、在飞追加追赶、LWW 采纳与资产 delta 均由最终 67/67 覆盖；4518 后台冲刷链通过。
- **DEFERRED**：多标签真实并发、关闭标签后重开零丢失尚无浏览器级证据。今晚没有用正式 4517 和真实资产做破坏式关页实验；建议下一轮在隔离 data dir + 两个 4518 browser context 上补 LWW/pagehide 实测。

## 8. 最终放行

- `npm run verify:global`：**67/67 PASS**，Vite production build **73ms**。
- 4518 production：**15/15 PASS**；console error/warning、page error、失败请求、外联、`/api` 资源 **全 0**。
- 4518 性能：300/800 元素均 **214ms**，分别低于 900/1600ms 红线。
- 4517：按 launchd 合同 `kickstart -k` 后，状态 `registered=true / running=true / apiHealthy=true`，根页面与 `/api/graph` 均 HTTP 200。
- 4517 只读 Chrome：`canvasVisible=true`，console error/warning、page error、request failed **全 0**。
- Git：小步提交均在 `feat/scene-store`；本分支已推送；未 push main、未合并 PR、未发 Release。

## 给哥的总汇报

今晚真正做成的是：自研墨迹从“能画”跨到“能当工具用”。多选、缩放、旋转、复制、Alt 拖、图片、文字字号、橡皮和统一撤销都进入同一份场景文档；整理时墨迹不再瞬移，后台状态也终于可见但安静。结构上，FlowCanvas 进了 800 行硬门，v17 运行时残骸从主线退干净，公开叙事已完全切到 SceneStore + 自研墨迹。

明早最值得先看三处：顶部工具岛的完整墨迹流、整理时容器与墨迹同曲线、右上角同步状态点。下一步建议按证据价值排序：先补 352 节点 Performance trace，再补双标签/pagehide 隔离浏览器实测，最后做匿名 4518 hero 场景和 README GIF。三项都不该借真实 4517 画布冒险。

## 9. 2026-07-19 接管与 352 节点拖动 Performance trace

- **做了什么**：先按交接门槛重新核验 Git、全局回归、4517 与 4518；随后新增隔离 `performance-352` 场景，以确定性匿名数据生成 `1 街区 + 1 工作区 + 350 会话 = 352` 个 React Flow 节点，仍挂载真实 production `FlowCanvas`。浏览器以 held pointer 拖动街区标题栏 120 步，同时采集 rAF 帧间隔、Long Task 和原始 CDP Performance trace；报告与 trace 写入被 Git 忽略的 `output/acceptance/`。
- **CONFIRMED 证据**：`npm run verify:global` fresh 68/68；Chrome `150.0.7871.125` 上 352 节点完整，537 个 rAF 样本、等效 122.71fps（本机 120Hz）、p95 9.1ms、max 33.2ms、>20ms 慢帧 1/537（0.186%）；120 个 pointer move 形成 118 个不同位置，最终位移 200.08px；页面 PerformanceObserver 与 CDP `RunTask >=50ms` 均 0；console error/warning、page error、失败请求、外联、`/api` 资源全 0。trace 含 68,448 events，gzip 1,033,329 bytes，SHA-256 `f36996b9e1034a1a2858af6cb207e4ea4d0003d192073a80b9464b1cc5dbbb3a`；报告为 `output/acceptance/canvas-352-performance-20260719T124328-15104.report.json`。同一构建的 4518 production 15/15 复跑通过。
- **决策与取舍**：继续沿用 `frame p95 <=20ms / max <=50ms / 慢帧 <=5% / Long Task=0` 的既有严门，不用 300/800 挂载耗时替代拖动证据；取证页不复制 UI、reducer 或拖动逻辑，只提供匿名数据与浏览器观测探针。
- **失败尝试及原因**：接管时直接打开 4518 `/` 命中了 production 壳，因隔离服务拒绝 `/api/graph` 而出现 `Failed to fetch`；查明合法交互入口是 `/?mode=interaction` 后重跑为全零。该错误 URL 没有被算作产品失败，也没有通过放宽 CSP 绕过隔离。
- **未完成与最小下一步**：P1 双标签 LWW、daemon 暂停/恢复和 pagehide 仍无浏览器级证据；下一步只在临时 data dir 与临时端口上建立可重复场景，绝不触碰 4517 真实资产。

## 10. 双标签 LWW、daemon 重启与 pagehide 真实浏览器证据

- **做了什么**：新增临时 scene daemon 与匿名浏览器页，服务端只加载真实 `server/scene.mjs`，页面只复用 production `scene-store.js`、`api.js`、SSE 和 `TopBar`；验收器每次创建随机本机端口与临时 data dir。实现前的回归先证明 `flushNow()` 没有把 keepalive 传给 scene/files API（9/10 稳定失败），随后补齐 `SceneStore → App → api` 的 options 传递链。
- **CONFIRMED 证据**：Chrome `150.0.7871.125` 两个独立 browser context 的 writerId 不同；干净标签收到 rev 2 时 `accepted=true` 且静默显示 `tab-a-clean`。B 标签本地为 `tab-b-dirty` 时收到 A 的 rev 3，现场记录 `accepted=false / localText=tab-b-dirty / sync=dirty`，没有被远端静默覆盖；随后 B 后台写成为 LWW，双方收敛到 `tab-b-dirty`。临时 daemon PID `38762` 被真实终止后，页面进入 `error / Failed to fetch / live=false`，继续输入 `offline-continued` 仍同步可见；同一临时 data dir 由 PID `38948` 重启后自动回到 `saved/live=true` 并落盘。pagehide 用例在 300ms 防抖前立即关闭页面，服务端故意延迟 150ms，重开仍读到 `pagehide-final`。page error、外联、刷新对话框均 0；daemon 真停只产生 1 个预期同源失败请求。临时 data dir 最终已删除。机器报告：`output/acceptance/scene-sync-20260719T125620-38761.report.json`。
- **决策与取舍**：没有用真实 4517 或仓内 `data/` 做关页/停服实验，也没有用 mock reducer 证明自己；临时服务只保留生产 scene LWW/SSE 所需的最小路由。`flushNow` 同时给场景与新增图片资产显式 keepalive，避免“文字能保、刚贴的图丢失”的半修复。
- **失败尝试及原因**：第一次 verifier 用 `networkidle` 等页面，而 SSE 长连接使该条件永远不成立，90 秒后超时；改为 `load + __SYNC_ACCEPTANCE__.ready`。第二次被缺失 favicon 的 404 console error 拦下；临时服务对 favicon 返回 204 后全诊断通过。两次都未降低 LWW、离线或 pagehide 验收标准。
- **未完成与最小下一步**：P2 匿名 README hero 仍未制作；下一步新增专用匿名展示场景，拍静态高分辨率产品图并控制仓库体积，不复用性能 HUD。

## 11. 匿名 production FlowCanvas hero 与 README

- **做了什么**：在既有 4518 production 构建中新增 `?mode=hero`，只提供确定性匿名数据与克制构图，继续复用真实 `FlowCanvas + SceneStore + TopBar`。场景展示 8 张会话卡、1 个画板、2 张便签、5 个自研墨迹元素与真实 saved 同步点；没有性能/验收 HUD。新增可重复截图脚本，浏览器阻断外联，并扫描渲染文本和 HTML 中的用户路径、用户名、Claude/Codex 目录及 `r2://`。
- **CONFIRMED 证据**：Chrome `150.0.7871.125`、1440×900 截图 `docs/assets/agent-session-canvas-hero.png` 为 141,772 bytes，SHA-256 `431a39c7508479eb8a4ac794e13e55b2fd3b4377627be5fa106741bf01dc8770`。节点/墨迹/同步点数量全部与合同一致；console error/warning、page error、失败请求、外联、敏感字符串泄漏全 0。机器报告：`output/acceptance/anonymous-hero-20260719T130914-68398.report.json`。图片已由人工肉眼检查，README 使用仓内相对路径并明确匿名来源。
- **决策与取舍**：按 Apple Design 的层级与克制标准，只增加左上品牌岛和数据构图，不造第二套产品页、不加宣传专用功能；静态 PNG 足以真实展示产品，避免 GIF 的体积与动态噪音。141.8KB 在清晰度和仓库体积之间足够合理。
- **失败尝试及原因**：第一次截图脚本把 CSP wait predicate 写成直接表达式，Chrome 将其视为字符串求值并按 production CSP 拒绝；改成 Playwright 可注入的函数表达式后通过，没有放宽 CSP。最终复跑又发现 `animation-play-state: paused` 会把同步点冻结在不同相位，使两次 PNG 相差 6 bytes；截图专用样式改为关闭 animation/transition 后连续生成两次，字节数与 SHA 完全一致。
- **未完成与最小下一步**：P0/P1/P2 本身已收口；剩余只做整轮 fresh 复验、4517 只读浏览器健康、真实资产哈希与 Git/远端一致性确认，不再扩功能。

## 12. 2026-07-19 最终 fresh 复验与安全收口

- **全局与 4518**：提交后 `npm run verify:global` 为 71/71，Vite production build 75ms。production FlowCanvas 十五链 15/15；console error/warning、page error、失败请求、外联、`/api` 资源全 0。300/800 元素直渲均为 213ms，分别低于 900/1600ms 红线。
- **352 节点 Performance trace**：fresh 报告 `output/acceptance/canvas-352-performance-20260719T130832-66855.report.json` 为 PASS：509 个帧间隔样本、119.45fps、p95 9.1ms、max 33.3ms、>20ms 慢帧 1/509（0.196%）、页面/CDP Long Task 均 0；120 个 pointer move、118 个不同位置、位移 200.08px。trace 67,667 events、1,001,976 bytes，SHA-256 `2b6c163986dcea3ac46e4909752c8c6deeef5e166378dc3ad854a5f7c43a6731`。
- **双标签/pagehide**：fresh 报告 `output/acceptance/scene-sync-20260719T130959-70477.report.json` 为 PASS；两个 writer 独立，干净远端采纳、脏本地保护与最终 LWW 收敛成立；daemon 真停时输入继续，重启后自动追平；pagehide 重开仍为 `pagehide-final`。页面错误/外联/对话框与重开 console error/warning 均 0，临时 data dir 已删除。
- **4517 正式服务**：未改 server，无需 kickstart。launchd `registered=true / running=true / apiHealthy=true`，PID 68432；`/` 与 `/api/graph` 均 HTTP 200。fresh Chrome 只读打开得到 `canvasVisible=true / 343 nodes / 1 viewport / 1 sync point`，console error/warning、page error、失败请求全 0；没有执行坐标拖拽。
- **数据安全**：仓内真实 `data/{enrich,canvas,layout,drawing-files}.json` 的 SHA-256 与接管基线逐个相同，分别为 `cbcf7088…`、`2a4b6b3d…`、`cdb9d9c…`、`44136fa3…`；4518 已停止。`~/.claude`、`~/.codex` 未写入，main/PR/Release 均未操作。
- **失败尝试及原因**：最终 4517 headless 诊断第一次误用 `networkidle`，被正式 SSE 长连接持续占用；主动终止后改为 `load + .canvas-root visible + 1800ms`，全部诊断为 0。没有把测试等待错误记作产品失败，也没有关闭 SSE 迎合验收。
- **未完成与最小下一步**：功能和证据项均已完成；仅剩把本日志与可复现截图修正提交到 `feat/scene-store`、跑提交后全局回归并推送，远端 SHA 一致后停止。

## 13. 接力双工具选择、在飞关页与模型路由收口

- **做了什么**：把详情面板原先隐式沿用源工具的“带接力开新会话”改为两个并列、无默认高亮的直接入口：`Claude Code 接力会话` 与 `Codex 接力会话`。两者复用同一份自包含接力提示词，只切换 `launch.tool`，`cwd/sourceKey/mode/prompt` 保持一致。4518 新增 production `HandoffLaunchChoices` 匿名夹具，分别用键盘和指针驱动两家入口，不拉真实终端。继续收口上一轮审计暴露的两个链路缺口：SceneStore 在普通 flush 已在飞时让 pagehide 并发发送最新 `clientSeq`，服务端拒绝同 writer 的旧请求倒灌；模型路由兼容 Codex 成功退出但只在 stdout 给最终文本的行为，并把 Claude fallback 改用稳定 `sonnet` 别名。
- **CONFIRMED 证据**：`npm run verify:global` fresh 76/76；4518 production 入口与原十五链全绿，新增接力选择报告为 `choices=[claude,codex] / samePrompt=true / noDefaultLaunch=true / keyboardAndPointer=true`，全部浏览器诊断为 0。隔离 launcher 用假 `open` 验证两家都生成 prompt 模式命令，且两个临时 prompt 文件逐字相同，不创建真实 Claude/Codex 会话。scene-sync fresh 浏览器报告 `output/acceptance/scene-sync-20260719T140113-84999.report.json` 真实观察到 `pendingSceneWrites=1` 后再输入并关页，重开仍为 `pagehide-inflight-final`；双标签、断服输入、恢复追平继续通过。352 fresh trace 为 574 帧样本、123.15fps、p95 9.4ms、max 33.4ms、Long Task 0；300/800 均 214ms。4517 按合同 kickstart 到 PID 99277，根页/graph 200，fresh 只读 Chrome 为 344 nodes / 1 viewport / 1 sync point，console error/warning、page error、失败请求与外联全 0；kickstart 前后四份珍贵数据哈希逐项相同。
- **决策与取舍**：没有把某一家做成 primary，也没有先弹选择再多一次确认——两个入口一眼可见、一次点击完成。`bingo-agent-handoff` 的事实包保持 provider-neutral，目标工具只是运输层。真实终端拉起会创建用户会话并写两家资产，因此验收使用 production 组件 payload + 隔离 launcher 假终端，既验证完整命令链又不越过数据边界。模型路由同理用临时 PATH 假 CLI 覆盖 Codex stdout 与 Claude fallback，不消耗真实额度。
- **失败尝试及原因**：新增浏览器夹具最初以 `.js` 保存 JSX，被 Vite 严格解析门拒绝；改为 `.jsx` 后通过，没有放宽构建配置。随后误把 300/800 verifier 指向 production 4518，三个等待探针的进程被显式 SIGTERM；对应 Playwright 临时 profile 已自动删除，改按合同分别启动 `acceptance:prod` 与 `acceptance:canvas` 后通过。这两次都是验收编排失败，不计作产品 PASS，也没有遗留 4518/Chrome 进程。
- **未完成与最小下一步**：本项功能与隔离证据已完成；提交前只剩最终 76/76 全局门、Git diff/状态检查、4518 停止和分支推送一致性确认。

## 14. 同级审查整改与 A–G 全仓终验

- **做了什么**：先把接力按钮候选 `860c6b5` 交给同级独立审查员，只把结论当审查输入。审查得到 PARTIAL 后逐项整改：给 lineage 持久化目标 `tool` 并覆盖 Claude→Codex 跨工具连边；为超过 keepalive 上限的场景增加 localStorage 场景快照与 IndexedDB 图片恢复仓；把 4518 墨迹链扩到 P/R/O/A/T/E、文字与图片变换及 drop；把 352 场景改成 `1 街区 + 1 工作区 + 349 会话 + 1 便签` 并依次拖动三个目标；300/800 改采 20 个真实 React 更新帧；详情与终端框错误态改为自动退避重连。同步清理当前事实漂移：`.loop` 旧 backlog 移入 history、配置改为 v18/v19，公开文档移除已不存在的 overrides，`npm ci` 清除旧绘图库遗留的 extraneous 依赖。
- **CONFIRMED 代码证据**：`npm run verify:global` fresh **81/81 PASS**，Vite production build **82ms**；`npm ls --depth=0` 仅有 React/React DOM/React Flow/Vite/plugin-react 五个顶层包，`npm ci` 报 0 vulnerabilities；`FlowCanvas.jsx` **798 行**；主线源码与公开文档旧绘图库残词为 0（`tests/archive/` 与冻结历史计划保留）。跨工具 lineage、旧 lineage 兼容、pagehide 大载荷、图片 fileIds/writer 隔离与旧在飞 clientSeq 条件清理均有新增 node:test。
- **CONFIRMED production 浏览器证据**：fresh `--suite prod` 通过 production 壳、墨迹全链和接力双入口；`choices=[claude,codex] / samePrompt=true / noDefaultLaunch=true / keyboardAndPointer=true`。墨迹 18 项检查全 true；console error/warning、page error、外联与 `/api` 资源全 0。fresh 匿名 hero 在 1440×900 in-app Chrome 肉眼复核：15 个 React Flow 节点、5 个墨迹、1 个同步点、横纵溢出均 0；双入口截图显示两按钮同级、无默认高亮。
- **CONFIRMED 性能证据**：fresh 报告 `output/acceptance/canvas-352-performance-20260719T144846-59822.report.json` 为 PASS。街区/工作区/便签各 120 个 pointer move，分别产生 118/117/117 个不同位置并位移 200.08/199.21/199.21px；p95 9.2/9.1/8.9ms，max 41.8/24.7/33.2ms，慢帧比例 0.382%/0.201%/0.267%，页面与 CDP Long Task 都为 0；trace 198,900 events、gzip 2,913,186 bytes、SHA-256 `0847442d41a357f6a13659731464d7a588150b59a64de30288862c02732d0839`。300/800 各 20 个更新帧，p95 8.5/8.7ms、max 8.6/12.2ms、Long Task 0，所有浏览器诊断为 0。
- **CONFIRMED 双标签/pagehide 证据**：fresh 报告 `output/acceptance/scene-sync-20260719T150534-12586.report.json` 为 PASS。两个 writer 独立；干净采纳、脏本地保护、最终 LWW 收敛成立；临时 daemon 真停时输入继续且状态为 error，重启后自动 saved；普通关页、在飞关页和 216,021 UTF-8 字节大载荷关页重开都保留最终编辑，大载荷先 `localRecoveryApplied=true`，再由正常冲刷落盘；追平后再关闭并重开为 `cleanReopenRecoveryApplied=false`，证明旧 clean 快照不会伪装成新恢复。每条恢复记录只加载自身引用 fileIds；清理恢复同时受同 writer `clientSeq` 与 `savedAt` 上界保护，旧在飞回执不能删除更新记录，只有记录确实清除才清对应 IDB 资产。服务端更新时间直接来自原子替换后 `canvas.json` 的 mtime，没有增加第五份持久化真相。临时 data dir 已删除，page error、外联、对话框和重开 console error/warning 都为 0。
- **CONFIRMED 4517 与数据安全**：server 变更后按合同 kickstart，launchd 为 `registered/running/apiHealthy=true`、PID 83120；`/` 与 `/api/graph` HTTP 200，已有 `canvas.json` mtime 直接返回非零 `sceneUpdatedAt=1784442817254.9658`。fresh 只读 Chrome 为 343 nodes / 1 viewport / 1 sync point，无横纵溢出，console error/warning、page error、失败请求全 0；没有任何坐标操作。kickstart 前后 `data/{enrich,canvas,layout,drawing-files}.json` SHA-256 逐项相同：`5b17c84b… / 3efe7bfe… / 17ce13e9… / 44136fa3…`。`~/.claude`、`~/.codex` 未写入，未操作 main/PR/Release。
- **决策与取舍**：大载荷恢复不把 base64 图片正文塞进 localStorage；场景快照只保存逻辑文档，刚导入图片先异步暂存 IndexedDB，服务端成功后再清。视觉品质用 production hero + 真实交互状态做人工复核，但“哥一眼觉得更整齐”仍明确留给用户最终主观验收，不伪装成自动化 PASS。
- **失败尝试及原因**：图片验收最初使用 SVG，Chrome 的 `createImageBitmap(File)` 在该夹具路径未完成解码，改用确定性 120×80 PNG 后通过；352 工作区最初按节点高度 12% 落点，因 349 个子会话让它命中内部会话卡，改为工作区头部固定 20px 后产生真实位移；修正后的首个 trace 有一次 194.1ms 调度空洞但 Long Task 为 0，未放宽 max≤50ms 红线，fresh 重跑三目标全部通过。上述失败均保留为失败，不计入 PASS。
- **未完成与最小下一步**：功能、性能、稳定性、文档、结构与正式服务证据已经收口；只剩把本候选绑定到最终 SHA，交同级审查员复审、按结论修正后推送 `feat/scene-store`。若复审发现新问题，不以当前自验替代独立结论。

## 15. 同级复审发现图片竞态后的反例修复

- **做了什么**：同级审查员对 `7973407` 给出 PARTIAL，并确定性复现“图片资产上传成功即清 IndexedDB → 另一 writer 写空场景裁掉服务端孤儿 → 原场景失败/关页后只剩 fileId”的数据安全竞态。本轮删除 `persistFiles` 成功即清理，改由 SceneStore 把本次 delta 的 `confirmedFileIds` 交给对应 `persistScene`，只有 `/api/scene` 成功回执后才清本地图片正文。4519 增加真实图片反例：A 的资产上传成功但场景连续被拒，B 的 LWW 空图提交真实裁掉孤儿，随后停止 daemon 并关闭 A；同一 browser context 重开后必须从 IndexedDB 取回图片、重新先传资产再交场景，确认后恢复仓为空。同时补齐 A 组先前未被 production 浏览器逐项驱动的 Shift 加选/移除、批量描边+填充+线宽、批量删除 undo 与 Esc 分层收工具。
- **CONFIRMED 证据**：`npm run verify:global` fresh **82/82 PASS**，Vite production build **82ms**。4518 `--suite prod` 的 **21 项**墨迹检查全 true；接力仍为 `choices=[claude,codex] / samePrompt=true / noDefaultLaunch=true / keyboardAndPointer=true`，console error/warning、page error、失败请求、外联与 `/api` 资源均 0。scene-sync fresh 报告 `output/acceptance/scene-sync-20260719T152855-84447.report.json` 为 PASS：图片场景先进入 `error`、另一 writer 裁掉服务端孤儿、daemon 真停退出码 0；重开 `recoveryApplied=true / imagePresentAfterReopen=true`，服务端最终同时存在 1 个 image 元素和 `sync-acceptance-image-file` 正文，并在 scene 回执后观测到 `recoveryAssetClearedAfterSceneReceipt=true`；故障注入页只有预期的同源 500 console error 1 条，重开恢复页 console error/warning 均 0，临时 data dir 已删除。
- **CONFIRMED 性能证据**：fresh 352 报告 `output/acceptance/canvas-352-performance-20260719T152211-59513.report.json` 与 trace 为 PASS，gzip SHA-256 `b13bdb11bd22b7eb9f93fc3a3bf4c44df29eb5d2cf98d70ef9cede18cbdc8049`。街区/工作区/便签 p95 为 9.1/9.1/8.9ms，max 为 33.4/33.3/24.9ms，三者各 120 pointer move、位移约 199–200px，页面与 CDP Long Task 都为 0，所有浏览器诊断为 0。300/800 各 20 个更新帧，挂载 20/23ms，p95 9.1/9.0ms，max 9.2/9.1ms，Long Task 0。
- **决策与取舍**：没有通过取消服务端孤儿裁剪掩盖竞态，也没有把图片反例降成 mock；保留“资产先行、全量场景 LWW、孤儿随场景裁剪”合同，只把恢复资产的确认点移到真正能证明引用落盘的 scene 回执之后。恢复过程仍在后台，不给任何输入路径增加网络或 IndexedDB 等待。
- **失败尝试及原因**：新增批量样式验收第一次点了第 5 个填充色却断言第 4 个色值，产品已正确写回但夹具等待超时；修正索引后 21 项全绿。性能复验时曾把 300/800 尺寸 verifier 指向 production 入口，该入口不加载尺寸探针而持续等待；显式中止后按合同切换 `acceptance:canvas`，没有把空等待算作结果，也没有放宽阈值。
- **未完成与最小下一步**：把本轮代码、反例、文档提交为新候选 SHA，再交同一位同级审查员绑定 SHA 复审；只有复审无代码级阻断、4517/端口/Git 末态仍健康，才推送 `feat/scene-store`。B/C 的最终个人视觉偏好继续由用户验收，不伪装成机器事实。

## 16. 深交错复审与服务端保守资产策略

- **做了什么**：候选 `5da9cb3` 再次交同级审查后得到 FAIL。审查员证明“只把 IDB 清理推迟到 scene 回执”仍不充分：A 的图片 scene 已成功且本地正文已清，A 再产生脏编辑；B 后写空图会即时裁掉服务端图片，A 因 dirty 拒绝远端采纳，下一次 flush 又因 delta 基线认为图片早已上传而不重传，最终永久报“图片不在仓内”。这不是一个清理时点 bug，而是“全量 LWW + 脏本地保护”与“普通写立即做全局破坏性 GC”的合同冲突。本轮删除 `scene.write` 的即时孤儿裁剪：内容寻址、同 ID 不可变资产在普通 LWW 写入中保守留存；未来若需要 GC，必须另做能证明没有其他脏标签引用的显式保守策略。
- **反例升级**：4519 不再只测“首次 scene 从未成功”。新链要求 A 图片场景先成功并现场确认 IDB 已清；随后 A 再 dirty，B 从已含图场景删除图片并以空图成为 LWW，服务端 canvas 已空但内容寻址正文仍在；A 现场记录 `accepted=false` 且本地图片仍在，再停止 daemon、关闭 A、重启同一临时 data dir。重开必须 `recoveryApplied=true`，并在没有 IDB 正文的前提下用服务端保留资产恢复图片与最终脏编辑。
- **CONFIRMED 证据**：升级后的 `tests/drawing-files.test.mjs` 真实以 writer B 写空图后确认 `f1/f2` 正文仍在，writer A 随后引用 `f1` 的场景可直接成功。fresh Chrome 报告 `output/acceptance/scene-sync-20260719T153733-9267.report.json` 为 PASS：`firstSceneSaved=true / localRecoveryAssetClearedAfterFirstScene=true / dirtyRemoteAdoptionAccepted=false / assetRetainedAfterOtherWriterEmptyScene=true / recoveryApplied=true / imagePresentAfterReopen=true`；最终服务端同时有 1 个 image 元素和 `sync-acceptance-image-file` 正文。故障注入页只有预期的同源 500 console error 1 条，fresh 重开 console error/warning、page error、外联和对话框均 0，临时 data dir 已删除。
- **CONFIRMED 全局与正式服务**：最终 `npm run verify:global` fresh **82/82 PASS**，Vite production build **101ms**。按合同 `launchctl kickstart -k` 后 4517 为 `registered/running/apiHealthy=true`、PID 11778，根页与 `/api/graph` HTTP 200；fresh 只读 Chrome 为 343 nodes / 1 viewport / 1 sync point，无横纵溢出，console error/warning、page error、失败请求、外联全 0。kickstart 前后四份珍贵数据 SHA-256 完全相同：`5b17c84b… / 3efe7bfe… / 17ce13e9… / 44136fa3…`；4518/4519 均已停止。
- **决策与取舍**：数据安全优先于自动回收少量内容寻址孤儿。普通 LWW 写者无法观察其他标签的脏本地文档，因此没有安全资格删除全局资产；保留正文只增加可恢复的磁盘占用，不会破坏现有 canvas/layout/drawing-files 格式，也不在交互路径增加等待。
- **未完成与最小下一步**：提交新 SHA 并再次绑定同级复审；只有审查无代码级 blocker 才推送目标分支。`5da9cb3` 明确是失败候选，不作为完成证据。

## 17. 手绘/文字真实可见性与顶栏快捷键

- **做了什么**：按用户真实反馈不再只看 SceneStore 对象数量，先用 computer-use 在 4518 production `FlowCanvas` 手动画线、切 V 选择、点 T 输入文字。定位两个独立缺口：`freedraw.points` 是相对点，`InkLayer` 却漏掉元素 `x/y` 平移，导致命中/选择框在落笔处而笔迹飞到世界原点；文字 textarea 在真实 `pointerdown` 挂载后又被浏览器默认焦点动作立刻 blur，空元素随即删除。修复手绘 SVG `translate(x y)`，文字落点阻止默认焦点回抢；顶部工具按钮直接显示 `N/B/V/P/R/O/A/T/E/F` 键帽。
- **CONFIRMED 功能证据**：独立 Microsoft Edge 的 computer-use 修后实测中，橙色笔迹与鼠标拖动位置重合，V 点中后蓝色八向选择框/旋转柄可见，真实鼠标点 T 后 textarea 保持焦点，输入 `ABC` 并切回 V 后 SVG 文字仍可见。fresh 4518 `--suite prod` 为 PASS，26 项墨迹检查全 true；新增 `inkVisualPlacement / textVisualPlacement / selectionRingVisible / trustedPointerTextInput / visibleShortcutLabels` 全 true，其中 trusted pointer 用浏览器真鼠标点击后确认 textarea 是 `document.activeElement`，中文「真实鼠标文字可见」提交后有非透明包围盒。production 壳、Claude/Codex 双接力与 console error/warning、page error、失败请求、外联、`/api` 资源继续全绿/全 0。
- **CONFIRMED 性能证据**：fresh 352 节点报告 `output/acceptance/canvas-352-performance-20260719T161434-6076.report.json` 为 PASS；街区/工作区/便签各 120 个 pointer move，p95 均为 9.1ms，max 为 41.6/30.5/26.1ms，位移约 199–200px，页面/CDP Long Task 都为 0；trace gzip SHA-256 `ac6d5f5c2630881e980b9853f6d7456a0ad7cb1ce2a8e264531a8fb547caf35f`。300/800 元素挂载 21/25ms，20 个更新帧 p95 9.0/8.6ms、max 9.3/9.2ms、Long Task 0。
- **CONFIRMED 4517 与数据安全**：本轮未改 server，无需 kickstart；launchd 为 `registered/running/apiHealthy=true`、PID 11778，根页和 `/api/graph` 都是 HTTP 200。Edge 只读打开 4517 已看到顶栏全部键帽；独立 headless 只读诊断为 343 nodes / 1 viewport / 1 sync point / 横纵溢出 0，console error/warning、page error、失败请求全 0，没有对 4517 做坐标操作。四份珍贵数据 SHA-256 与本轮修复前完全相同：`5b17c84b… / 3efe7bfe… / 17ce13e9… / 44136fa3…`；4518 已停止。
- **决策与取舍**：没有迁移或修改真实 `canvas.json`，因为它的 5 条 freedraw 均是合法相对点且 opacity=100，缺口纯属渲染。键帽直接放在现有按钮内，不新造帮助面板；`FlowCanvas.jsx` 仍为 799 行，未越 800 行红线。
- **失败尝试及原因**：旧 4518 只断言元素和 DOM 存在，所以误把错位笔迹与合成文字当成 PASS；新验收改为比较实际 `getBoundingClientRect` 与 flow 坐标。第一版把缩放后笔迹高度红线写成 `>15px`，实测正好 15px 而失败；保留坐标误差 <5px 的核心门，只把有效可见高度改为 >10px 后通过。computer-use 的 CJK 键盘注入在 Edge 只输入了 ASCII 前缀，这是自动输入法限制；人机可见链用 `ABC` 取证，中文内容另由同一 production textarea 的 trusted pointer + `fill` 取证，没有把未输入的中文伪报为 computer 结果。
- **未完成与最小下一步**：最终 `npm run verify:global` 已为 **82/82 PASS**，Vite production build 92ms；功能、可见性、性能与正式服务证据已收口。剩余只是候选 SHA 复审、提交推送与远端一致性确认。

## 18. 智能整理从“无碰撞”升级为可扫读地图

- **做了什么**：用户实测指出旧整理虽无重叠但仍乱且不美观。本轮把工作区瀑布流改为按活跃度从左上开始的行对齐网格；高层街区与画板共同进入按目标宽高比 1.6 自动选择的 1–4 条等宽平衡车道，巨型街区只拉长自己的车道。显式整理会压缩过大的画板并持久化街区/画板终点，容器墨迹沿原 520ms 曲线随行，便签保留手工位置，人工归属不变且整个文档一笔撤销。顶栏入口改名“智能整理”并写清边界。
- **CONFIRMED 证据**：新增 4518 `layout-quality` 匿名乱序场景，真实 production `FlowCanvas + SceneStore + TopBar` 点击按钮后为 2 条车道、5 个容器、DOM 容器包络 2098×1404（aspect 1.494）；直接读取 `.react-flow__node[data-id]` 验证工作区活跃度行序、零容器碰撞、画板参与排布并压缩、街区终点持久化、墨迹随行、便签不动、归属保留和撤销恢复全部为 true。随后让一条已有后继的车道首街区真实增长，DOM 观测到目标变高、后继下移、后继墨迹同 delta 随行且仍零碰撞；再真实拖动后继街区右下缩放手柄，存储坐标先追平视觉投影，墨迹前后位置差不超过 4px。修复后 production fresh 全绿，同步规划 3.6ms、目标节点 transform 提交 26.3ms、Long Task 0；四段 console error/warning、page error、失败请求、外联与 `/api` 资源全 0。真实 4517 只读数据代入显示旧整理包络 5124×8623（aspect 0.59），新显式规划的容器包络约 7374×4610（aspect 1.60），未在正式画布执行写操作。
- **决策与取舍**：日常扫描/增量生长仍尊重手工锚点；只有用户主动点“智能整理”才重排画板。便签不按手动连线源随行，因为连线只表达语义，不代表空间归属。
- **失败尝试及原因**：第一版固定画板只让自动街区避让，真实数据仍被旧画板锚点拉成 5488×9178 的细长条，不能称为变整齐；第二版让关联便签跟随连线源，真实反例把两张便签推到约 `y=-2900`，因此撤销该方案。首个候选又被同级 review 找出已持久化街区增长后可能互相重叠及验收复用 `buildGraph` 自证，分别改为稳定避碰 growth 回归和独立 DOM 探针。第二轮 review 继续找出扩张区墨迹漏认领、后继容器墨迹脱离与夹具 DOM 读取污染 Long Task：最终保留 live 尺寸，用只读投影同步携带墨迹；基线与全量 DOM 读取移出产品计时窗，布局性能场景收敛为 10 个工作区的代表构图，不放宽 Long Task=0。第三轮 review 又用真实缩放手柄发现 `onResize` 绕过投影提交，后继墨迹会瞬跳 117.11px；修复为街区/画板 resize 写入前先原子提交投影，并把同一反例固化进 production 自动门。最终 4517 只读浏览器首次等待 `networkidle` 因常驻 SSE 超时，改按 `domcontentloaded + 4s` 观察，不把超时算通过。
- **最终复验**：同级独立 review 对代码候选 `06e5d319cb953d8f1dc54d6b14d04e3795ecc17f` 给出 PASS，A–G 均为 CONFIRMED。`npm run verify:global` 为 90/90；production 四段连续两次 fresh PASS，布局同步 3.6/3.8ms、首帧 27.0/26.6ms、Long Task 0。fresh 352 报告 `output/acceptance/canvas-352-performance-20260719T180442-58184.report.json` 为 PASS，街区/工作区/便签 p95 为 9.8/9.8/10.0ms、max 为 26.5/25.0/24.9ms、页面/CDP Long Task 均为 0，trace 194,168 events、gzip 2,822,732 bytes、SHA-256 `56bc320b38cf8a3c36f8455405bdfd001785a13f3c65befb003a76579649c6fc`。300/800 挂载 24/23ms，20 帧更新 p95 均 9.2ms、Long Task 0。scene-sync 报告 `output/acceptance/scene-sync-20260719T180531-60994.report.json` fresh PASS 且临时 data dir 已删除。4517 launchd/API healthy，根页/graph 200；只读 Chrome 为 341 nodes / 1 viewport / 1 sync dot / 1 智能整理入口，无页面溢出，console/page/request/external 全 0。浏览器夹持前后四份珍贵数据哈希不变；`canvas/layout` 的较早运行时变化保留为用户/daemon 活数据，未回滚。
- **未完成与最小下一步**：功能与证据缺口已收口，代码候选同级 review 已 PASS，本证据更新也已提交；仅剩推送 `feat/scene-store` 并核对远端 SHA。个人审美仍由用户最终验收。

## 19. 空闲抢帧修复与真实拓扑性能闸门

- **做了什么**：针对用户反馈“总是卡卡的”，先在 4517 只读现场与 4518 隔离环境复现，再定位到 12 条关系线的 `stroke-dashoffset` 和 171 个活跃状态点的永久动画：即使不操作，3 秒 trace 也产生约 360 次 `UpdateLayoutTree`、360 次 `PrePaint` 与 4680 次 `Paint`。本轮保留颜色/虚线/状态语义，移除常驻动画；同时把 352 节点夹具从只有节点数量的稀疏场景升级为 1 街区、12 工作区、335 会话、3 画板、1 便签、171 活跃状态点、22 条关系线与 3 个墨迹元素，并新增空闲 CDP trace 硬门。
- **CONFIRMED 证据**：修复前新增回归先按预期失败，指出 `.dot.active` 仍有 `animation`；修复后 `tests/drawing.test.mjs` 16/16。review 修正后的 fresh 352 报告 `output/acceptance/canvas-352-performance-20260719T214053-79616.report.json` 为 PASS：1500ms 空闲窗运行中动画 0，`UpdateLayoutTree / PrePaint / Paint` 全 0；街区/工作区/便签拖动 p95 为 11.1/10.8/10.7ms，max 28.5/33.7/32.8ms，按住位移 199.21/174.0/199.21px，松手跳变全 0，页面 Long Task 全 0；CDP 实际识别 46,969 个 `ThreadControllerImpl::RunTask`，其中 ≥50ms 为 0。空闲 trace SHA-256 `523d7e43ee3e8833f21e725ed6404e79fb1d9ad40c76331c407dff72cf7eab1b`，拖动 trace SHA-256 `84d88e15867d5f0a81d596825c634c25d4d6b9d722354589c75814121af91097`。300/800 挂载 21/27ms、20 帧 p95 8.5/8.6ms、Long Task 0。production 26 项墨迹、Claude Code/Codex 双接力与智能整理全链均 PASS；scene-sync fresh 报告 `output/acceptance/scene-sync-20260719T213313-71025.report.json` 覆盖两个独立 browser context、断服恢复和普通/在飞/216KB pagehide，临时 data dir 已删除。
- **浏览器实测**：computer-use 只在 4518 驱动 production `FlowCanvas`。手动点击 P 后画出橙色不透明笔迹，切 V 点击可见选择框、八向手柄与旋转柄；点击 T 后真实 textarea 获得焦点，输入 `ABC 123 visible text` 并切回 V 后文字与选择框可见。顶栏 N/B/V/P/R/O/A/T/E/F 键帽均可见；fresh 页面点击原生验收按钮后 26 项全部 PASS。正式 4517 只做只读浏览器检查：336 nodes、22 edges、171 active dots、6 个墨迹 DOM，运行中动画 0，1500ms `UpdateLayoutTree / PrePaint / Paint` 全 0，无横纵溢出，console error/warning、page error、失败请求与外联全 0。
- **决策与取舍**：根因是空闲阶段持续抢占主线程与绘制，不是 SceneStore 输入路径等待；因此没有重构画布，也没有靠放宽拖动阈值掩盖问题。同步 `saving` 点仍保留短暂动画，因为它只在后台冲刷期间出现且不构成永久空闲成本；常态 active 与关系语义改为静态视觉。352 场景继续复用 production `FlowCanvas`，只增强确定性匿名数据和取证，不复制第二套 UI。
- **失败尝试及原因**：首次误在 `acceptance:prod` 服务上运行 `--suite canvas`，`?size=300` 命中错误入口而等待探针，人工中止后切换到正确的 `acceptance:canvas`，300/800 全通过；computer-use 在已经手改状态的页面点击原生验收按钮一直停在 running，fresh reload 后同一按钮正常 PASS。首个候选 `ac3fb0346c888214cc8ac14709484c9946413f01` 又被同级独立 review 判为 PARTIAL：工作区斜拖撞入相邻成员后松手向下打包约 949px，却被旧 `位移≥150px` 反向奖励；Chrome 150 trace 中可用的兼容任务名已是 `ThreadControllerImpl::RunTask`，只找 `RunTask` 会让 CDP Long Task 恒为 0。新门先按预期卡住同一瞬跳，再把工作区改为向同容器合法空隙纵向拖动，并新增按住位移上/下限、松手跳变 ≤8px、CDP 任务事件非零覆盖及双事件名识别，重跑松手跳变为 0、CDP 实际覆盖 46,969 个任务事件。
- **最终复审与数据安全**：同级独立 reviewer 对修正候选 `605ec792c9b6d65cea0b2abd419a02f6d358297c` 给出 PASS，独立复算 trace 为 46,969 个兼容任务事件、最大 32.704ms、≥50ms 为 0，并确认三目标松手跳变全 0；`npm run verify:global` 为 91/91。未改 server、无需 kickstart，4517 launchd 仍为 `registered/running/apiHealthy=true`、PID 721，根页与 `/api/graph` 200。`canvas/layout/drawing-files` 哈希与本轮前一致；`enrich.json` 在正式 daemon 21:20 自动扫描期间发生运行时变化，验收只发 GET、未调用写接口，未回滚或覆盖这份珍贵活数据。4518 与 scene-sync 均为隔离数据。仅剩提交本证据更新、推送 `feat/scene-store` 并核对远端 SHA；未跨 main、PR、Ready、Release 边界。
