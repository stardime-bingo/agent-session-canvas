# 夜间冲刺日志

> 施工时间：2026-07-18 夜间；2026-07-19 继续收口证据缺口
>
> 分支：`feat/scene-store`
>
> 边界：未改写 `data/`、`~/.claude`、`~/.codex`；未操作 main、PR、Release；4517 只做构建生效、规范 kickstart 与只读验收。

## 0. 开工基线

- **做了什么**：逐字对齐 `docs/顶级状态计划.md`、根/前后端 CLAUDE 成员图与今天十笔提交；确认分支和正式 4517 健康后开工。
- **证据**：开工 `npm run verify:global` 为 65/65；4517 根页面与 `/api/graph` 均为 HTTP 200。
- **取舍**：先补 A 组高频手感，再修整理/同步，最后清结构和公开叙事；不在真实 4517 做任何坐标自动化。

## 1. 墨迹选中手感补全

- **做了什么**：新增 `ink-selection.js` 纯内核；选择态支持框选、Shift 多选、批量移动/删除/改样式、八向缩放、共同中心旋转、Cmd/Ctrl+C/V、Alt 拖复制。绑定宿主与文字按闭包一起变换，所有连续手势都 coalesce 为一步 undo。
- **证据**：提交 `c3018eb feat(ink): 补齐多选缩放旋转与复制手感`；选择纯函数测试和 4518 production FlowCanvas 链通过；该笔 `verify:global` 71/71。
- **取舍**：几何、闭包、ID/绑定重映射都留在纯函数，不把选择规则塞回 FlowCanvas；ClipboardEvent 同时写私有 MIME 与 text fallback，支持浏览器内跨会话画布粘贴。
- **未竟**：没有承诺跨应用通用矢量格式；当前跨会话指同一应用/画布上下文中的系统剪贴板。

## 2. 图片粘贴与拖入

- **做了什么**：图片文件粘贴/拖入时同步放可见占位，FileReader、解码和 SHA-256 内容寻址全部后台完成；成功后回填既有 `drawingFiles` 仓，失败显示元素级错误；用户先 undo 时，迟到异步结果不能复活元素。
- **证据**：提交 `fe9199d feat(ink): 图片粘贴拖入即时占位后台入仓`；图片尺寸、稳定内容 ID、同步占位测试通过；4518 production 图片链通过；该笔 `verify:global` 74/74。
- **取舍**：图片原始内容不先进入场景引用，继续遵守“资产先行、引用后到”；交互路径没有 await。
- **未竟**：没有加入裁剪、滤镜、压缩策略，这些不属于本轮顶级状态判据。

## 3. 整理同曲线与同步状态

- **做了什么**：接通已有但未使用的 batch carry 逆向 FLIP 桥；整理时所属墨迹先钉回旧像素，再与容器用同一 420ms 曲线滑到新位。SceneStore 同步状态改成稳定快照并单独订阅，TopBar 增加克制的 saved/dirty/saving/error 状态点。
- **证据**：提交 `f76ca2c fix(canvas): 整理墨迹同曲线并亮同步状态`；新增状态订阅与整理桥回归；该笔 `verify:global` 76/76。
- **取舍**：没有改写整理布局算法和人工归属，只修“节点滑、墨迹跳”的表征裂缝；状态点不弹窗、不阻塞、不要求用户重试。
- **未竟**：尚未用一组真实审美基线重新裁定“活跃者靠前”的整体排布节奏。

## 4. 橡皮与 4518 验收闭环

- **做了什么**：加入 `E` 橡皮工具；按住划过同步命中顶层元素，一整笔合并为一步 undo。4518 扩为十五链：冷渲、连发即时、DOM 同步、V/P/E 快捷键、框选多选、批量移动、缩放、旋转、复制粘贴、Alt 拖、图片粘贴、橡皮撤销、删除撤销、后台冲刷、console clean。
- **证据**：提交 `e9b013b feat(ink): 加入单笔橡皮并清除旧运行时`；4518 production 15/15，console error/warning、page error、失败请求、外联、`/api` 资源全部为 0。
- **取舍**：第一次夹具运行暴露了“旋转复制件盖住原件而断言盯错对象”的假失败；改用空间唯一的导入图片验证顶层命中，没有改生产命中规则去迎合测试。
- **未竟**：橡皮当前删除整元素，不做路径分割；这是本轮保持简单和可撤销性的明确边界。

## 5. 性能红线与结构卫生

- **做了什么**：恢复 300/800 两档自研墨迹直渲门；把连接说明、容器命中与绘图菜单移入既有纯模块；删除主线残留的旧绘图库相机/手势/样式运行时与对应失效测试；`FlowCanvas.jsx` 从 857 行降到 798 行。
- **证据**：4518 Chrome 冷挂载 300 元素 214ms（红线 900ms）、800 元素 214ms（红线 1600ms），DOM 数量精确一致且所有浏览器诊断为 0；`wc -l web/src/canvas/FlowCanvas.jsx` = 798；主线 `rg` 旧绘图库品牌/运行时残词为 0（只排除 `tests/archive/`、冻结历史计划与本任务书）。
- **取舍**：全局测试数从中途 76 降到最终 67，是删除 9 条已无生产入口的 v17 相机事务测试，不是隐藏失败；历史合同完整保留在 `tests/archive/`。
- **未竟（DEFERRED）**：352 节点拖动 60fps 还没有真实 Performance trace。不能拿 300/800 墨迹挂载代理冒充；建议新增 4518 的 352 节点专用场景并录 trace，仍不触碰 4517 坐标。

## 6. 文档与开源脸面

- **做了什么**：README 重写自研 SVG 墨迹、单场景文档、零等待、全套操作和本地图片资产说明；Codex Marketplace release notes 改成 v18/v19；PRIVACY/TERMS 明确内容寻址图片和本地后台持久化；根 AGENTS/CLAUDE、web/server 成员图同步；4518 文档改为十五链。
- **证据**：提交 `20078e9 docs: 用自研墨迹重写公开叙事`；主线残词扫描清零；公开文档与代码当前功能逐项对齐。
- **取舍**：没有伪造产品截图，也没有拿含真实会话的 4517 截图进仓。
- **未竟（DEFERRED）**：README 截图/GIF 尚未更新。建议在 4518 做一套专门的匿名 hero fixture 后再拍，避免真实会话泄露，也避免把验收夹具当产品广告图。

## 7. 稳定性证据边界

- **CONFIRMED**：SceneStore 的同步 mutate、coalesce undo/redo、防抖冲刷、失败无限退避、在飞追加追赶、LWW 采纳与资产 delta 均由最终 67/67 覆盖；4518 后台冲刷链通过。
- **DEFERRED**：多标签真实并发、关闭标签后重开零丢失尚无浏览器级证据。今晚没有用正式 4517 和真实资产做破坏式关页实验；建议下一轮在隔离 data dir + 两个 4518 browser context 上补 LWW/pagehide 实测。

## 8. 最终放行

- `npm run verify:global`：**67/67 PASS**，Vite production build **73ms**。
- 4518 production：**15/15 PASS**；console error/warning、page error、失败请求、外联、`/api` 资源 **全 0**。
- 4518 性能：300/800 元素均 **214ms**，分别低于 900/1600ms 红线。
- 4517：按 launchd 合同 `kickstart -k` 后，状态 `registered=true / running=true / apiHealthy=true`，根页面与 `/api/graph` 均 HTTP 200。
- 4517 只读 Chrome：`canvasVisible=true`，console error/warning、page error、request failed **全 0**。
- Git：小步提交均在 `feat/scene-store`；本分支已推送；未 push main、未合并 PR、未发 Release。

## 给哥的总汇报

今晚真正做成的是：自研墨迹从“能画”跨到“能当工具用”。多选、缩放、旋转、复制、Alt 拖、图片、文字字号、橡皮和统一撤销都进入同一份场景文档；整理时墨迹不再瞬移，后台状态也终于可见但安静。结构上，FlowCanvas 进了 800 行硬门，v17 运行时残骸从主线退干净，公开叙事已完全切到 SceneStore + 自研墨迹。

明早最值得先看三处：顶部工具岛的完整墨迹流、整理时容器与墨迹同曲线、右上角同步状态点。下一步建议按证据价值排序：先补 352 节点 Performance trace，再补双标签/pagehide 隔离浏览器实测，最后做匿名 4518 hero 场景和 README GIF。三项都不该借真实 4517 画布冒险。

## 9. 2026-07-19 接管与 352 节点拖动 Performance trace

- **做了什么**：先按交接门槛重新核验 Git、全局回归、4517 与 4518；随后新增隔离 `performance-352` 场景，以确定性匿名数据生成 `1 街区 + 1 工作区 + 350 会话 = 352` 个 React Flow 节点，仍挂载真实 production `FlowCanvas`。浏览器以 held pointer 拖动街区标题栏 120 步，同时采集 rAF 帧间隔、Long Task 和原始 CDP Performance trace；报告与 trace 写入被 Git 忽略的 `output/acceptance/`。
- **CONFIRMED 证据**：`npm run verify:global` fresh 68/68；Chrome `150.0.7871.125` 上 352 节点完整，537 个 rAF 样本、等效 122.71fps（本机 120Hz）、p95 9.1ms、max 33.2ms、>20ms 慢帧 1/537（0.186%）；120 个 pointer move 形成 118 个不同位置，最终位移 200.08px；页面 PerformanceObserver 与 CDP `RunTask >=50ms` 均 0；console error/warning、page error、失败请求、外联、`/api` 资源全 0。trace 含 68,448 events，gzip 1,033,329 bytes，SHA-256 `f36996b9e1034a1a2858af6cb207e4ea4d0003d192073a80b9464b1cc5dbbb3a`；报告为 `output/acceptance/canvas-352-performance-20260719T124328-15104.report.json`。同一构建的 4518 production 15/15 复跑通过。
- **决策与取舍**：继续沿用 `frame p95 <=20ms / max <=50ms / 慢帧 <=5% / Long Task=0` 的既有严门，不用 300/800 挂载耗时替代拖动证据；取证页不复制 UI、reducer 或拖动逻辑，只提供匿名数据与浏览器观测探针。
- **失败尝试及原因**：接管时直接打开 4518 `/` 命中了 production 壳，因隔离服务拒绝 `/api/graph` 而出现 `Failed to fetch`；查明合法交互入口是 `/?mode=interaction` 后重跑为全零。该错误 URL 没有被算作产品失败，也没有通过放宽 CSP 绕过隔离。
- **未完成与最小下一步**：P1 双标签 LWW、daemon 暂停/恢复和 pagehide 仍无浏览器级证据；下一步只在临时 data dir 与临时端口上建立可重复场景，绝不触碰 4517 真实资产。

## 10. 双标签 LWW、daemon 重启与 pagehide 真实浏览器证据

- **做了什么**：新增临时 scene daemon 与匿名浏览器页，服务端只加载真实 `server/scene.mjs`，页面只复用 production `scene-store.js`、`api.js`、SSE 和 `TopBar`；验收器每次创建随机本机端口与临时 data dir。实现前的回归先证明 `flushNow()` 没有把 keepalive 传给 scene/files API（9/10 稳定失败），随后补齐 `SceneStore → App → api` 的 options 传递链。
- **CONFIRMED 证据**：Chrome `150.0.7871.125` 两个独立 browser context 的 writerId 不同；干净标签收到 rev 2 时 `accepted=true` 且静默显示 `tab-a-clean`。B 标签本地为 `tab-b-dirty` 时收到 A 的 rev 3，现场记录 `accepted=false / localText=tab-b-dirty / sync=dirty`，没有被远端静默覆盖；随后 B 后台写成为 LWW，双方收敛到 `tab-b-dirty`。临时 daemon PID `38762` 被真实终止后，页面进入 `error / Failed to fetch / live=false`，继续输入 `offline-continued` 仍同步可见；同一临时 data dir 由 PID `38948` 重启后自动回到 `saved/live=true` 并落盘。pagehide 用例在 300ms 防抖前立即关闭页面，服务端故意延迟 150ms，重开仍读到 `pagehide-final`。page error、外联、刷新对话框均 0；daemon 真停只产生 1 个预期同源失败请求。临时 data dir 最终已删除。机器报告：`output/acceptance/scene-sync-20260719T125620-38761.report.json`。
- **决策与取舍**：没有用真实 4517 或仓内 `data/` 做关页/停服实验，也没有用 mock reducer 证明自己；临时服务只保留生产 scene LWW/SSE 所需的最小路由。`flushNow` 同时给场景与新增图片资产显式 keepalive，避免“文字能保、刚贴的图丢失”的半修复。
- **失败尝试及原因**：第一次 verifier 用 `networkidle` 等页面，而 SSE 长连接使该条件永远不成立，90 秒后超时；改为 `load + __SYNC_ACCEPTANCE__.ready`。第二次被缺失 favicon 的 404 console error 拦下；临时服务对 favicon 返回 204 后全诊断通过。两次都未降低 LWW、离线或 pagehide 验收标准。
- **未完成与最小下一步**：P2 匿名 README hero 仍未制作；下一步新增专用匿名展示场景，拍静态高分辨率产品图并控制仓库体积，不复用性能 HUD。

## 11. 匿名 production FlowCanvas hero 与 README

- **做了什么**：在既有 4518 production 构建中新增 `?mode=hero`，只提供确定性匿名数据与克制构图，继续复用真实 `FlowCanvas + SceneStore + TopBar`。场景展示 8 张会话卡、1 个画板、2 张便签、5 个自研墨迹元素与真实 saved 同步点；没有性能/验收 HUD。新增可重复截图脚本，浏览器阻断外联，并扫描渲染文本和 HTML 中的用户路径、用户名、Claude/Codex 目录及 `r2://`。
- **CONFIRMED 证据**：Chrome `150.0.7871.125`、1440×900 截图 `docs/assets/agent-session-canvas-hero.png` 为 141,841 bytes，SHA-256 `b82ae114db22bf297a4e192255e119ea00f6bcc77991c0b6e05d719b1b53b07a`。节点/墨迹/同步点数量全部与合同一致；console error/warning、page error、失败请求、外联、敏感字符串泄漏全 0。机器报告：`output/acceptance/anonymous-hero-20260719T130558-61719.report.json`。图片已由人工肉眼检查，README 使用仓内相对路径并明确匿名来源。
- **决策与取舍**：按 Apple Design 的层级与克制标准，只增加左上品牌岛和数据构图，不造第二套产品页、不加宣传专用功能；静态 PNG 足以真实展示产品，避免 GIF 的体积与动态噪音。141.8KB 在清晰度和仓库体积之间足够合理。
- **失败尝试及原因**：第一次截图脚本把 CSP wait predicate 写成直接表达式，Chrome 将其视为字符串求值并按 production CSP 拒绝；改成 Playwright 可注入的函数表达式后通过，没有放宽 CSP。
- **未完成与最小下一步**：P0/P1/P2 本身已收口；剩余只做整轮 fresh 复验、4517 只读浏览器健康、真实资产哈希与 Git/远端一致性确认，不再扩功能。
