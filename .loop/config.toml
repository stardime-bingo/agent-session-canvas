target = "/Users/bingowu/BINGO-Space/AI-code/AGENT会话管理看板"
app_desc = "本地 Agent 会话指挥塔：React Flow 卡片地形与自研 SVG 墨迹共用 SceneStore 和单相机的融合画布"
modules = ["shared", "web/src/canvas", "web/src/App.jsx", "web/src/api.js", "server", "tests", "scripts", "docs"]
quality_bar = "v18/v19 当前合同：SceneStore 是 layout/edges/notes/boards/drawing/drawingFiles/seq 单一真相源，自研 SVG 墨迹与卡片共用 React Flow 相机；输入只做同步内存 mutate，网络、磁盘、图片读取和冲刷均在后台；每个候选 npm run verify:global 全绿，交互和性能只在隔离 4518 执行，4517 仅允许只读打开与 console 检查，禁止坐标操作；禁止测试改写真实 data、Claude/Codex 会话资产"
default_lane = "guarded"
max_attempts = 8
claim_ttl_seconds = 1800

[gates.focused]
build = "npm run build"
behavior = "npm test"

[gates.global]
build = "npm run build"
behavior = "npm test"
