target = "/Users/bingowu/BINGO-Space/AI-code/AGENT会话管理看板"
app_desc = "本地 Agent 会话指挥塔：React Flow 卡片地形与 Excalidraw 绘图的单相机融合画布"
modules = ["web/src/canvas", "web/src/App.jsx", "tests"]
quality_bar = "单一视口树；浮沉层级稳定；编辑事务原子换帧；无真实画布自动坐标拖拽；node:test、Vite build、隔离画布行为验收与实机 console 全绿"
default_lane = "guarded"
max_attempts = 3
claim_ttl_seconds = 1800

[gates.focused]
build = "npm run build"
behavior = "node --test tests/drawing.test.mjs"

[gates.global]
build = "npm run build"
behavior = "npm test"
